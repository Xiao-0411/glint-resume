# 识光简历运行指南

当前架构：前端在 Cloudflare Pages，FastAPI、MySQL 和 Cloudflare Tunnel 在本机运行。

<a id="toc"></a>
## 快速目录

- [1. 快速启动](#quick-start)
- [2. 首次配置](#first-setup)
- [3. 发布前端](#deploy-frontend)
- [4. Cloudflare Tunnel](#cloudflare-tunnel)
- [5. 验证与排错](#troubleshooting)

> 在 VS Code 中按住 `Ctrl` 点击目录链接即可跳转。

---

<a id="quick-start"></a>
## 1. 快速启动

### 本地开发

先在 phpstudy 启动 MySQL，然后双击：

```text
一键启动.bat
```

它会启动后端、前端和爬虫；检测到 `cloudflared` 时也会尝试启动 Cloudflare Tunnel。

访问：

```text
前端：http://localhost:5173
接口文档：http://127.0.0.1:8000/docs
```

### 线上完整运行

Cloudflare Pages 不需要本机运行 Vite，但必须保持以下链路：

```text
MySQL → FastAPI:8000 → Cloudflare Tunnel → api.sgjl.cloud
```

终端 1，启动后端：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\backend"
.\.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

终端 2，启动 Tunnel：

```powershell
cloudflared tunnel --config "$env:USERPROFILE\.cloudflared\config-glint.yml" run glint-backend
```

可以关闭本地 Vite 和普通 Git 终端。不能关闭 MySQL、FastAPI、Tunnel，也不能让电脑休眠。

[返回目录](#toc)

---

<a id="first-setup"></a>
## 2. 首次配置

### 数据库

在 phpMyAdmin 导入：

```text
backend\sql\001_session_persistence_mysql.sql
```

### 后端

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\backend"
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
```

`backend\.env` 至少确认：

```env
DATABASE_URL=mysql+pymysql://root:你的MySQL密码@localhost:3306/glint?charset=utf8mb4
APP_ENV=production
CORS_ORIGINS=https://sgjl.cloud,https://www.sgjl.cloud
AUTH_SECRET_KEY=请替换为至少32字符的长随机字符串
RATE_LIMIT_PER_MIN=30
```

LLM、管理员、SMTP 等可选配置直接参考 `backend\.env.example`。

### 三平台爬虫

`eatmoreduck/boss-zhipin-scraper` v2.2.0 已集成到 `backend\vendor\boss-zhipin-scraper`。三个平台共用同一个隔离 Chrome 登录态：BOSS 使用 vendor 的 CDP 抓取，智联和猎聘通过原生 CDP 读取各自搜索页面。首次使用先安装后端依赖：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\backend"
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

然后从项目根目录运行：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历"
.\启动登录浏览器.bat
```

脚本会打开 BOSS 直聘、智联招聘和猎聘三个页面。分别完成登录；只有三个页面都能显示可爬取的职位列表时脚本才会自动退出。登录态保存在专用 profile，不会复制主 Chrome 的密码、历史记录或扩展。然后正常启动 `backend\run_crawler.py`。可在 `backend\.env` 调整：

```env
CRAWLER_CITIES=北京,上海,广州,深圳,杭州,成都,武汉,西安
CRAWLER_MAX_CITIES=4
CRAWLER_DETAIL_TIMEOUT_SECONDS=45
BOSS_SCRAPER_CITY=上海
BOSS_SCRAPER_PAGES=1
BOSS_SCRAPER_MAX_KEYWORDS=5
BOSS_SCRAPER_CDP_PORT=9222
BOSS_SCRAPER_TIMEOUT_SECONDS=90
CRAWLER_REQUEST_TIMEOUT_SECONDS=180
CRAWLER_INTERVAL_SECONDS=60
CRAWLER_MAX_KEYWORDS=5
```

`CRAWLER_INTERVAL_SECONDS` 以秒为单位，默认值 `60` 表示每 60 秒运行一轮；修改后需重启爬虫。每个平台只跑前 5 个关键词，并轮询北京、上海、广州、深圳四个城市；页面选择其他地点时按所选城市实时抓取。列表任务只抓摘要，用户打开职位时再按需抓取完整 JD 并写回数据库，后续列表刷新不会清空已保存详情。`启动登录浏览器.bat` 会启动三个爬虫共用的隔离 Chrome；不要再使用旧的主 Chrome profile 复制方式。

完整启动方式：

```text
1. 运行 启动登录浏览器.bat，在专用 Chrome 中分别登录 BOSS直聘、智联招聘和猎聘。
2. 等待终端显示三个网站全部就绪并自动退出，然后保持该专用 Chrome 窗口运行；关闭它会同时关闭 CDP 端口 9222。
3. 运行 一键启动.bat，或单独运行 backend\run_crawler.py。
```

`一键启动.bat` 只负责检查 CDP 是否已启动，不会复制 Cookie，也不会自动关闭主 Chrome。若未启动 CDP，后端和前端仍会启动，但三个招聘爬虫都会记录失败状态。

### 前端

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\frontend"
npm install
Copy-Item .env.example .env.local
```

本地环境：

```env
VITE_USE_BACKEND=true
VITE_API_BASE_URL=http://localhost:8000
```

生产环境 `frontend\.env.production`：

```env
VITE_USE_BACKEND=true
VITE_API_BASE_URL=https://api.sgjl.cloud
```

[返回目录](#toc)

---

<a id="deploy-frontend"></a>
## 3. 发布前端

只提交前端，避免把本地后端和爬虫改动一起发布：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历"
git add frontend
git status --short
git diff --cached --stat
git commit -m "描述本次前端修改"
git push origin main
```

不要直接使用 `git add .`。

Cloudflare Pages 设置：

```text
Production branch: main
Root directory: frontend
Build command: npm run build
Build output directory: dist
```

Root 已经是 `frontend`，输出目录不能写成 `frontend/dist`，否则会查找错误的：

```text
frontend/frontend/dist
```

部署成功后访问：

```text
https://sgjl.cloud
```

页面未更新时先检查 Cloudflare Deployments，再使用 `Ctrl + F5`。

[返回目录](#toc)

---

<a id="cloudflare-tunnel"></a>
## 4. Cloudflare Tunnel

### 配置文件

```text
%USERPROFILE%\.cloudflared\config-glint.yml
```

格式：

```yaml
tunnel: glint-backend 的 UUID
credentials-file: C:\Users\你的用户名\.cloudflared\对应UUID.json

ingress:
  - hostname: api.sgjl.cloud
    service: http://localhost:8000
  - service: http_status:404
```

验证配置：

```powershell
cloudflared tunnel --config "$env:USERPROFILE\.cloudflared\config-glint.yml" ingress validate
```

### DNS

在 `sgjl.cloud` 的 Cloudflare DNS 页面配置：

```text
Type: CNAME
Name: api
Target: <GLINT_TUNNEL_UUID>.cfargotunnel.com
Proxy status: Proxied
TTL: Auto
```

只填写 Name `api`。不要让其他域名的 `cert.pem` 创建出：

```text
api.sgjl.cloud.其他域名
```

### 启动

```powershell
cloudflared tunnel --config "$env:USERPROFILE\.cloudflared\config-glint.yml" run glint-backend
```

日志中的 Tunnel ID 必须属于 `glint-backend`。单条 QUIC 连接重试可以忽略，只要仍有其他连接显示 `Registered tunnel connection`。

[返回目录](#toc)

---

<a id="troubleshooting"></a>
## 5. 验证与排错

### 健康检查

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
Invoke-RestMethod https://api.sgjl.cloud/api/health
```

两者都应返回：

```json
{"status":"ok","llm_available":true}
```

### `api.sgjl.cloud` 返回 522

按顺序检查：

1. phpstudy MySQL 是否运行。
2. 本地 `/api/health` 是否正常。
3. Tunnel 是否使用 `config-glint.yml`。
4. 日志中的 Tunnel ID 是否正确。
5. DNS `api` 是否指向 glint Tunnel 的 `cfargotunnel.com`。

本地正常、Tunnel 有连接但仍为 522，通常就是 DNS 记录仍指向旧源站。

### Pages 构建失败

报错：

```text
Output directory "frontend/frontend/dist" not found
```

修正：

```text
Root directory: frontend
Build output directory: dist
```

`npm audit`、没有 `functions` 目录、chunk 超过 500 kB 都只是警告。

### 前端接口失败

- 本地使用 `http://localhost:8000`。
- 线上使用 `https://api.sgjl.cloud`。
- 修改 Vite 环境变量后必须重启或重新构建。

[返回目录](#toc)
