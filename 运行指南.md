# 识光简历运行指南

这份文档按当前项目状态写：后端是 FastAPI + MySQL，前端是 Vue/Vite，注册使用邮箱验证码。

## 0. 前置环境

需要先准备：

| 工具 | 用途 | 验证命令 |
| --- | --- | --- |
| phpstudy MySQL | 后端数据库 | phpstudy 面板里 MySQL 显示运行中 |
| Python 3.10+ | 后端 | `python --version` |
| Node.js 18+ | 前端 | `node -v` |
| npm | 前端依赖 | `npm -v` |

下面命令都用 PowerShell 执行。

## 1. 数据库

1. 打开 phpstudy，启动 MySQL。
2. 打开 phpMyAdmin，导入这个文件：

```text
C:\Users\xiaoweizhong\Desktop\识光简历\backend\sql\001_session_persistence_mysql.sql
```

这个 SQL 会创建 `glint` 数据库和这些表：

```text
users
email_verification_codes
sessions
messages
resumes
llm_usage_records
```

如果你已经导入过旧版本 SQL，也可以不用删库。后端启动时会自动创建缺失的新表。

## 2. 后端配置

进入后端目录：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\backend"
```

如果还没有虚拟环境：

```powershell
python -m venv .venv
```

安装依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

如果没有 `.env`，复制示例：

```powershell
Copy-Item .env.example .env
```

打开 `backend\.env`，重点检查这几项：

```env
DATABASE_URL=mysql+pymysql://root:你的phpstudy_mysql密码@localhost:3306/glint?charset=utf8mb4
PORT=8000
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
```

权限系统的超级管理员账号由后端 `.env` 管理。首次启动或每次重启后，后端会按这里的配置创建/同步超级管理员：

```env
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=Admin123456
SUPER_ADMIN_DISPLAY_NAME=超级管理员
```

账号角色规则：

```text
super_admin：可管理管理员和普通用户，可将普通用户升级为管理员
admin：只能管理普通用户
user：普通用户，所有注册账号默认都是该角色
```

LLM token 记录和费用控制默认开启。可按需要调整每日额度：

```env
LLM_USAGE_ENABLED=true
LLM_DAILY_CALL_LIMIT_PER_USER=100
LLM_DAILY_TOKEN_LIMIT_PER_USER=200000
LLM_DAILY_COST_LIMIT_USD_PER_USER=5
```

如果使用的模型价格不是默认 Claude/OpenAI 常见价格，手动覆盖每百万 token 单价：

```env
LLM_INPUT_PRICE_PER_1M_USD=3
LLM_OUTPUT_PRICE_PER_1M_USD=15
```

登录后可在 Swagger 里查看当前用户用量：

```text
GET /api/usage/llm/today
GET /api/usage/llm/recent
```

如果你暂时不想配置真实邮箱 SMTP，又想本地能注册账号，把这个打开：

```env
EMAIL_VERIFICATION_DEV_MODE=true
```

打开后，发送验证码接口会把验证码返回给前端，仅限本地开发。正式使用必须改回：

```env
EMAIL_VERIFICATION_DEV_MODE=false
```

真实邮箱验证码需要配置 SMTP，例如 QQ 邮箱：

```env
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USERNAME=你的邮箱@qq.com
SMTP_PASSWORD=邮箱授权码
SMTP_FROM=你的邮箱@qq.com
SMTP_FROM_NAME=识光简历
SMTP_USE_SSL=true
```

注意：`SMTP_PASSWORD` 通常是邮箱授权码，不是邮箱登录密码。

## 3. 启动后端

在 `backend` 目录执行：

```powershell
.\.venv\Scripts\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

看到下面类似输出就是成功：

```text
Uvicorn running on http://127.0.0.1:8000
Application startup complete.
```

验证后端：

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/health" -Method Get
```

也可以打开：

```text
http://127.0.0.1:8000/docs
```

## 4. 前端配置

新开一个 PowerShell 窗口，进入前端目录：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\frontend"
```

安装依赖：

```powershell
npm install
```

如果没有 `.env.local`，复制示例：

```powershell
Copy-Item .env.example .env.local
```

确认 `frontend\.env.local` 是：

```env
VITE_USE_BACKEND=true
VITE_API_BASE_URL=http://localhost:8000
```

`VITE_USE_BACKEND=true` 很重要。现在登录、注册、会话恢复都依赖后端。

## 5. 启动前端

在 `frontend` 目录执行：

```powershell
npm run dev
```

看到类似输出就是成功：

```text
VITE ready
Local: http://localhost:5173/
```

浏览器打开：

```text
http://localhost:5173/
```

## 6. 正常启动顺序

每次开发建议按这个顺序：

```text
1. phpstudy 启动 MySQL
2. 启动后端
3. 启动前端
4. 浏览器打开 http://localhost:5173/
```

后端和前端要分别占用一个终端窗口，不要在同一个窗口里连续执行。

## 7. 常见问题

### Q1: 后端报 Unknown database 'glint'

说明数据库没建好。重新导入：

```text
backend\sql\001_session_persistence_mysql.sql
```

或者确认 `DATABASE_URL` 里的数据库名是 `glint`。

### Q2: 后端报 Access denied for user 'root'

说明 `DATABASE_URL` 里的 MySQL 密码不对。去 phpstudy 里确认 root 密码，然后改：

```env
DATABASE_URL=mysql+pymysql://root:正确密码@localhost:3306/glint?charset=utf8mb4
```

### Q3: 后端启动命令提示 uvicorn 找不到

用这个命令，不要直接敲 `uvicorn`：

```powershell
.\.venv\Scripts\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

### Q4: 注册时提示“邮件服务未配置”

原因是 `EMAIL_VERIFICATION_DEV_MODE=false`，但 SMTP 没填。

本地临时调试可以改：

```env
EMAIL_VERIFICATION_DEV_MODE=true
```

改完后要重启后端。

如果要真实发邮件，就填写 `SMTP_HOST`、`SMTP_USERNAME`、`SMTP_PASSWORD` 等配置。

### Q5: 前端打开后接口失败

检查：

```env
VITE_USE_BACKEND=true
VITE_API_BASE_URL=http://localhost:8000
```

然后重启前端。Vite 的 `.env.local` 修改后必须重启。

### Q6: 端口被占用

查看端口：

```powershell
Get-NetTCPConnection -LocalPort 8000,5173 -State Listen
```

结束占用进程：

```powershell
Stop-Process -Id 进程ID -Force
```

### Q7: 前端依赖错误或页面空白

在 `frontend` 目录重新安装：

```powershell
npm install
npm run dev
```

### Q8: 后端依赖错误

在 `backend` 目录重新安装：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```
### Q9:后端端口占用

以后再遇到同样问题，可以先查端口：

  Get-NetTCPConnection -LocalPort 8000 -State Listen

然后停掉占用进程：

  Stop-Process -Id 进程ID -Forc

## 8. 快速命令汇总

后端：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\backend"
.\.venv\Scripts\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

前端：

```powershell
cd "C:\Users\xiaoweizhong\Desktop\识光简历\frontend"
npm run dev
```

访问：

```text
http://localhost:5173/
```
