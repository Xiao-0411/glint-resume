# 前端生产环境变量（Cloudflare Pages 构建时使用）
VITE_USE_BACKEND=true
# 留空 = 同源相对路径 /api/*，由 nginx 反代到后端(deploy/nginx/sgjl.cloud.conf)。
# 不要填 https://api.sgjl.cloud —— 该子域名未配置回源，Cloudflare 会返回 1033。
VITE_API_BASE_URL=